---
id: 111
title: "VFS"
part: "IX"
supermoduleId: 9
estimatedMinutes: 55
resources:
  - type: book
    title: "Linux Kernel Development (Robert Love)"
    url: "https://www.oreilly.com/library/view/linux-kernel-development/9780768696974/"
  - type: article
    title: "Kernel Newbies"
    url: "https://kernelnewbies.org/"
  - type: article
    title: "LWN.net — Linux Weekly News"
    url: "https://lwn.net/"
---
# VFS

## Why This Matters

The kernel is the core of Linux. Understanding its internals lets you debug, optimize, and reason about system behavior from first principles.

**VFS** sits within Linux Kernel Internals (Supermodule 9). This module covers 5 interconnected topics: dentries, inodes, superblocks, file objects, path resolution. Each builds on the previous, forming a coherent picture of how the Linux kernel works at this level.

## Core Concepts

### Dentries

**Dentries** is a foundational concept within vfs. Inside the kernel and driver subsystems, this concept directly affects system stability, performance, and correctness. Getting it wrong can mean kernel panics, data corruption, or security vulnerabilities. In practice, understanding dentries allows you to reason about system behavior rather than treating it as a black box.

### Inodes

**Inodes** is a foundational concept within vfs. Inside the kernel and driver subsystems, this concept directly affects system stability, performance, and correctness. Getting it wrong can mean kernel panics, data corruption, or security vulnerabilities. In practice, understanding inodes allows you to reason about system behavior rather than treating it as a black box.

### Superblocks

**Superblocks** is a foundational concept within vfs. Inside the kernel and driver subsystems, this concept directly affects system stability, performance, and correctness. Getting it wrong can mean kernel panics, data corruption, or security vulnerabilities. In practice, understanding superblocks allows you to reason about system behavior rather than treating it as a black box.

### File objects

**File objects** is a foundational concept within vfs. Inside the kernel and driver subsystems, this concept directly affects system stability, performance, and correctness. Getting it wrong can mean kernel panics, data corruption, or security vulnerabilities. In practice, understanding file objects allows you to reason about system behavior rather than treating it as a black box.

### Path resolution

**Path resolution** is a foundational concept within vfs. Inside the kernel and driver subsystems, this concept directly affects system stability, performance, and correctness. Getting it wrong can mean kernel panics, data corruption, or security vulnerabilities. In practice, understanding path resolution allows you to reason about system behavior rather than treating it as a black box.

## Practical Example

```bash
# Kernel introspection commands
$ uname -r                         # kernel version
$ cat /proc/version                # build info
$ zcat /proc/config.gz | grep SMP  # kernel config
$ dmesg | tail -20                 # kernel log
$ cat /proc/kallsyms | head        # kernel symbol table
$ ls /sys/module/                  # loaded modules
```

## Key Insights

- **Dentries** — understand this deeply and the rest of vfs follows naturally.
- **Inodes** — understand this deeply and the rest of vfs follows naturally.
- **Superblocks** — understand this deeply and the rest of vfs follows naturally.
- **File objects** — understand this deeply and the rest of vfs follows naturally.
- **Path resolution** — understand this deeply and the rest of vfs follows naturally.
- Think in terms of trade-offs: every design choice in the Linux kernel sacrifices something to gain something else.
- Build mental models, not memorized facts. The goal is to predict behavior from first principles.

## What Comes Next

The next module, **Block layer**, builds directly on these ideas. Requests and Queues extend what you've learned here into block layer.
